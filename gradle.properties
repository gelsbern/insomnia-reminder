# Basic Gradle/Fabric project settings
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Mod metadata
mod_version=2025.12.30-1
maven_group=net.nullstorm
archives_base_name=insomnia-reminder

# ---- Build target (compile against the LOWEST supported MC) ----
minecraft_version=1.21.6
yarn_mappings=1.21.6+build.1

# Loader (can be newer; this is just what you build with)
loader_version=0.16.9

# Fabric API for 1.21.6 (must exist on the maven)
fabric_version=0.123.2+1.21.6

# ModMenu + Cloth Config (these are fine to stay modern)
modmenu_version=11.0.3
cloth_config_version=15.0.140
